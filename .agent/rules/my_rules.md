---
trigger: always_on
---

- Follow https://docs.flutter.dev/app-architecture/recommendations
- After making any changes, run
  `dart fix --apply && dart format . && flutter analyze` and fix all analysis issues.
