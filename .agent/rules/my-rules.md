---
trigger: always_on
---

- After making any changes, run
  `dart fix --apply && dart format . && flutter analyze`.
- You must fix all analysis issues, not by suppressing them.
- Do not use `// ignore` comments or modify `analysis_options.yaml` to disable
  rules unless explicitly asked.
