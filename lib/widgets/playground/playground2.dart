import 'package:flutter/material.dart';

void main() => runApp(
      MaterialApp(
        home: Scaffold(
          body: _createBody(),
        ),
      ),
    );

Widget _createBody() => const SizedBox.shrink();
